export enum CameraTypes {
  PERSPECTIVE,
  ORTHOGRAPHIC,
}

export enum CursorTypes {
  DEFAULT = 'auto',
  CROSS = 'crosshair',
  POINTER = 'pointer',
  GRAB = 'grab',
  GRABBING = 'grabbing',
  PENCIL = 'url("/pencil.svg") 0 24, auto',
}
